# ============================================================================
# DISCORD BOT CONFIGURATION
# ============================================================================

# Discord bot token (REQUIRED)
# Get from: https://discord.com/developers/applications
DISCORD_TOKEN=your_bot_token_here

# Owner user ID (REQUIRED)
# Default: 978783908638375956
# Only this user can control the bot
OWNER_ID=978783908638375956

# ============================================================================
# TTS CONFIGURATION
# ============================================================================

# TTS Provider: gtts | google
# gtts = Free Google TTS (rate-limited)
# google = Google Cloud TTS (requires credentials)
TTS_PROVIDER=gtts

# Voice name (provider-specific)
# For gtts: not used (always uses default Spanish voice)
# For Google Cloud: e.g., "es-ES-Standard-A", "es-US-Standard-A"
VOICE_NAME=es-ES-Standard-A

# Language code
# Examples: es, es-ES, es-US, es-MX
LANG=es

# Maximum characters per TTS request
MAX_TTS_CHARS=200

# ============================================================================
# GOOGLE CLOUD TTS (OPTIONAL)
# ============================================================================

# Path to Google Cloud service account JSON file
# Only needed if TTS_PROVIDER=google
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Or set individual Google Cloud credentials:
# GOOGLE_CLOUD_PROJECT_ID=your-project-id
# GOOGLE_CLOUD_CLIENT_EMAIL=your-service-account@project.iam.gserviceaccount.com
# GOOGLE_CLOUD_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----

# ============================================================================
# LOGGING
# ============================================================================

# Log level: trace | debug | info | warn | error | fatal
LOG_LEVEL=info

# Pretty print logs (disable in production for JSON logs)
LOG_PRETTY=true

# ============================================================================
# VOICE SETTINGS
# ============================================================================

# Auto-join timeout (milliseconds)
VOICE_JOIN_TIMEOUT=10000

# Voice ready timeout (milliseconds)
VOICE_READY_TIMEOUT=20000

# Max reconnection attempts
VOICE_MAX_RETRIES=3

# Backoff base delay (milliseconds)
VOICE_BACKOFF_BASE=1000

# Backoff max delay (milliseconds)
VOICE_BACKOFF_MAX=10000

# Gemini (for /ia)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

GEMINI_MAX_TOKENS=2048

# Optional: comma-separated fallback models when quota is exhausted (429)
GEMINI_QUOTA_FALLBACK_MODELS=
